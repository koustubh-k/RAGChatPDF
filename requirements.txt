# --- Core Framework & UI ---
streamlit
langchain>=0.2.0  # Main LangChain package
langchain-core>=0.2.0  # Core abstractions, required by almost everything
langchain-community>=0.2.0 # Community integrations like document loaders

# --- Model & Embedding Integrations ---
langchain-groq>=0.1.5
langchain-huggingface>=0.0.3
langchain-ollama  # If you are using Ollama, otherwise remove

# --- Vector Store & Dependencies (Choose ONE) ---
# OPTION 1: For using ChromaDB
langchain-chroma>=0.0.9
chromadb>=0.4.22
pysqlite3-binary  # Resolves the sqlite3 dependency issue

# OPTION 2: For using FAISS
faiss-cpu
torch>=2.1.0  # The specific torch version you found to be stable

# --- Document Processing ---
pypdf
beautifulsoup4  # Used for WebBaseLoader in some RAG examples

# --- Utilities ---
python-dotenv
sentence-transformers # Pinned to a known stable version
